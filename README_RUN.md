# 🎣 Запуск бота для рыбалки

## 🚀 Способы запуска

### 1. Обычный запуск
```bash
python bot.py
```

### 2. Запуск с проверкой соединения
```bash
python bot.py --check-only
```

### 3. Запуск через прокси
```bash
python bot.py --proxy socks5://127.0.0.1:1080
```

### 4. Офлайн режим (для тестирования)
```bash
python bot.py --offline
```

## 🔧 Решение проблем с подключением

### Проблема: `[Errno 11001] getaddrinfo failed`
Это ошибка DNS - Telegram API недоступен.

**Решения:**
1. **Используйте прокси:**
   ```bash
   python bot.py --proxy socks5://127.0.0.1:1080
   ```

2. **Измените DNS:**
   - Windows: `nslookup api.telegram.org`
   - Используйте Google DNS (8.8.8.8) или Cloudflare DNS (1.1.1.1)

3. **Проверьте VPN/файрвол:**
   - Убедитесь что Telegram не заблокирован
   - Попробуйте отключить VPN

### Проблема: `Connection timed out`
Медленное соединение с Telegram API.

**Решения:**
1. **Увеличьте таймауты** (уже установлены на 30 секунд)
2. **Используйте прокси** для более стабильного соединения
3. **Попробуйте офлайн режим** для тестирования

## 🌐 Настройка прокси

### SOCKS5 прокси
```bash
python bot.py --proxy socks5://127.0.0.1:1080
```

### HTTP прокси
```bash
python bot.py --proxy http://127.0.0.1:8080
```

### HTTPS прокси
```bash
python bot.py --proxy https://127.0.0.1:8080
```

## 🔍 Диагностика

### Проверка соединения
```bash
python bot.py --check-only
```

### Тестирование API
```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

## 📱 Ожидаемый вывод при успешном запуске

```
🔍 Проверка подключения к Telegram API...
✅ Подключение успешно! Бот: @MDfish_bot
✅ Updater создан успешно
🎣 Бот для рыбалки запущен!
✅ Polling запущен успешно
```

## 🛠️ Офлайн режим

Для тестирования без интернета:
```bash
python bot.py --offline
```

В этом режиме:
- ✅ Бот запускается без проверки API
- ✅ Все функции работают локально
- ⚠️ Не будет отвечать на реальные сообщения

## 📞 Поддержка

Если ничего не помогает:
1. Проверьте токен бота в `config.py`
2. Убедитесь что бот имеет права на отправку сообщений
3. Попробуйте другой интернет-канал
4. Используйте офлайн режим для разработки

## 📋 Все доступные опции

```bash
python bot.py --help
```

Вывод:
```
Бот для рыбалки

optional arguments:
  -h, --help            show this help message and exit
  --proxy PROXY          URL прокси (например: socks5://127.0.0.1:1080)
  --offline              Офлайн режим для тестирования
  --check-only           Только проверить соединение
```
